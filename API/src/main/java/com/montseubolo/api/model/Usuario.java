package com.montseubolo.api.model;

import jakarta.persistence.Entity;

@Entity
public class Usuario {

    // TODO: adicionar os campos da entidade (id, nome, email, senha, ...)
    // e as anotacoes JPA correspondentes (@Id, @GeneratedValue, @Column, ...).

}
